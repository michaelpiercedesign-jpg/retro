// Mount point for the imperative showbox broadcast dock when ?coords= split layout is active.
export function ShowboxBroadcastPane() {
  return <div id="showbox-broadcast-mount" class="showbox-broadcast-mount" />
}
